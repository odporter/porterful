import { Metadata } from 'next'

// This provides SEO for the artist route
// For dynamic metadata based on artist data, we'd need a server component wrapper
export const metadata: Metadata = {
  title: 'Artist | Porterful',
  description: 'Support independent artists directly. 80% of every purchase goes to the creator.',
}
